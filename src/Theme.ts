import { extendTheme } from "@chakra-ui/react";

const theme = extendTheme ({
    fonts : {
        body: 'PlusJakarta Sans, sans-serif',
        heading: 'Jakarta Sans, sans-serif',
    },

    
})

export default theme